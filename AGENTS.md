# TankBattle專案結構

## 測試 (Testing)
- 撰寫測試，以達到接近 100% 的測試覆蓋率。每個公開函式、分支（branch）以及邊界情況（edge case）都應該有對應的測試。
- 所有測試都必須通過。不要提交任何會導致測試、Lint 或 Build 失敗的程式碼。

## 程式碼品質
- 撰寫乾淨、命名清楚、模組化的函式與類別。優先採用小型且職責單一的函式，而不是大型多用途函式。
- 純邏輯程式碼不應該包含 UI 在內。

## Git 工作流程 (Git Workflow)
- **worktrees**：當你修改的檔案可能也會被其他平行工作的 Agent 修改時，請使用 Git Worktree，避免這些修改互相衝突。
- **Commit often**：經常提交（Commit）。請將提交切分成小而專注的單位，當完成每個任務或一個有意義的工作單元時就提交。
- **Commit messages**：先從任務 ID、**使用者故事（User Story）**或 **Phase（階段）**開始，再清楚說明此次變更的內容與原因。
  範例：`feat(T003): 完成物理引擎`
- **Merge to main**：完成工作後，務必將變更合併回 main。不要留下停留在 Worktree 上而未合併的分支。
